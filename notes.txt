Tag vs Element
<h1>(tag open)
    componenets
</h1>(closing tag)
the whole open tag+components+close tag is called as elements.
1.html have only two things head and body.
2.shortcuts are known as emmets
ex:! or html:5--> to get alll html will come

PUSHING CODE INTO GITHUB
1.Intialize the git
   --> git init
2. git add .
3.git commit -m "message".


index2 notes 
1.a is for anchor Tag
2.target="_blank-->new tab
3.current local file-->../notes.txt
4.image tag-->img (it is an self closing tags)

13-04-24
    for normal spacing in paragraph use "&npsp"
    for emojis "windows+."
    block vs inline:
        BLOCK
        1.block elements usually won't allow any other element next to it
        2.block element contain height and width
        3.general block element is div,div:group the elements
        INLINE 
        1.example:span 