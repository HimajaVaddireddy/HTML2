Tag vs Element
<h1>(tag open)
    componenets
</h1>(closing tag)
the whole open tag+components+close tag is called as elements.
1.html have only two things head and body.
2.shortcuts are known as emmets
ex:! or html:5--> to get alll html will come

PUSHING CODE INTO GITHUB
1.Intialize the git
   --> git init
2. git add .
3.git commit -m "message".


index2 notes 
1.a is for anchor Tag
2.target="_blank-->new tab
3.current local file-->../notes.txt
4.image tag-->img (it is an self closing tags)

13-04-24
    for normal spacing in paragraph use "&npsp"
    for emojis "windows+."
    block vs inline:
        BLOCK
        1.block elements usually won't allow any other element next to it
        2.block element contain height and width
        3.general block element is div,div:group the elements
        INLINE 
        1.example:span 
17-04-24
    Forms
    1.placehholder: display before you enter something
    2.action after form takes you to new tab and name is taken in url.
    3.label and placeholder mostly not used at a time because both does the same work.
    4.shortcut-->input:file or input:date etc
    5.for in the label links with id in the input.
    6.in checkbox,radiobutton name should be same
    7.select can be used for single or multiple values
    8.there aare two ways to submit files :
     --->get
     --->post (we use method after form)//used when there is sensitive content
    
-----------------------------------------------
    23-4-24
-----------------------------------------------
1.   text decoration:none---takes underline from anchor tag
2.  display
        -->display is used to make elemenys appear diferently
        (i) display:block-->dont allow next elements side by side
        (ii)display:inline-->side by side
                          -->doesnt have height and width
        (iii)display:inline-block-->inludes height and width.
        (iv)display:table-->arranges in form of table
        (v)display:none-->removes elements